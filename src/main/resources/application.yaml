ktor:
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    host: 0.0.0.0
    port: 8080

database:
  jdbcUrl: jdbc:postgresql://localhost:5432/app
  username: app
  password: app
  maximumPoolSize: 10
